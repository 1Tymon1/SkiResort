SELECT 
    {[Measures].[Average weighted ride time]} ON COLUMNS,
    {([Ski Lift].[Lift Type].ALLMEMBERS)} ON ROWS
FROM 
    [Warehouse]
WHERE ([Date].[Hierarchy].[Year].&[2022])

--Find the average time spent on lifts of given type in 2022